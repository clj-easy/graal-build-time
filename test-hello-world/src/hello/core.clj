(ns hello.core
  "This package will be included in build init registration.
  It is included to test that:
   hello_world is not excluded because it happens to start with this package's name:
   hello")

(defn dummy [])
